# AGENTS.md

- このリポジトリは個人用 HTML ページ置き場です。
- 新規ページは `slug/index.html` 形式にします。
- ルートに HTML が置かれたら `publish-html-page` の手順で変換します。
- `pages.json` を必ず更新します。
- ページはスマホで見やすくします。
- 本当に秘密の情報は置きません。
- 変更後は必ず `git status` を確認します。
